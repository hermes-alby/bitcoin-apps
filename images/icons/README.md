# images/icons

- this folder contains SVG icons
- most icons are taken from [BitcoinDesign/BitcoinIcons](https://github.com/BitcoinDesign/Bitcoin-Icons/tree/main/optimized/filled) - some files are slightly modified to our needs (e.g. `send.svg` and `receive.svg`).
- some icons (e.g. `github.svg`) are taken from other sources.
